# gruted/openclaw-issues-status

Repository to store live GitHub issues export (issues.json) and README.


## Issues export

The file data/issues.json contains a live-exported JSON list of issues tagged with devops-ops-bot and stripe-demo (mocked for initial commit).
